// Backwards-compatible re-export.
// Keep this file so older imports continue working, but ensure we have a single implementation.
export { UserDetail, UserSignUpService as UserService } from './user-signup.service';
